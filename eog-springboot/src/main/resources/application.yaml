spring:
  application:
    name: eog-springboot

  datasource:
    url: jdbc:mysql://localhost:3307/minilog_db
    username: minilog_user
    password: dev_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # 개발 중 테이블 및 컬럼 구조가 자동으로 업데이트되도록 설정
    show-sql: true # SQL 쿼리 출력

# JWT를 생성 및 검증하는 데 사용할 비밀키 실무에서는 보안사고 대상이므로 반드시 하드코딩하지 않기
jwt:
  secret: IU+P6xyteCQmnKUTscLtAZsBQnIMABaqNIU8N+YC9mM=
